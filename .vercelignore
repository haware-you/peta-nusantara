# The deployable is the single built page. Everything else is source,
# tooling, or working documents that should not be served.
*
!peta-nusantara.html
!vercel.json

# Historical images shown on the entity cards ship alongside the page.
# SOURCES.md stays out — it is a working document, not a served asset.
!source images
!source images/*.jpg
!source images/*.JPG
!source images/*.png

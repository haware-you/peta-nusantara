# Source geometry — 2.9 MB, public domain, re-fetch with `npm run fetch`
data/ne50.geojson

# Generated by `npm run prep`, inlined into the build output
data/land.js

# Original downloaded source images for the Sources gallery. The real image
# data is embedded directly into the HTML by tools/embed-image.js, so these
# originals don't need to be tracked. Both the spaced and hyphenated names.
source images/
source-images/

node_modules/
.DS_Store
Thumbs.db
